"""Clap to Open — double-clap to launch your saved workspace layout.

A self-contained personal tool: a mic listener detects a double (or triple)
clap and replays a captured window layout, optionally with a startup sound.
All behaviour is driven by ``config.json`` in the project root and tuned
through a small local web control panel.
"""

__version__ = "1.0.0"
